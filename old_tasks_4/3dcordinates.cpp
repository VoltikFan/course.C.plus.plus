#include <iostream>

using namespace std;

int main()
{
    int x, y, z;
    cin >> x >> y >> z;

    long long antwort = ((x - 0) * (x - 0) + (y - 0) * (y - 0) + (z - 0) * (z - 0));

    cout << antwort;

    return 0;
}
